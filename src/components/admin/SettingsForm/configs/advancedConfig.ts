import { HardDrive } from 'lucide-react';
import { SectionConfig } from '../types';

export const advancedConfig: SectionConfig = {
  debugMode: {
    label: '디버그 모드',
    type: 'boolean',
    icon: HardDrive
  }
};
