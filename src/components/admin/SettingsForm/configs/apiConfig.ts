import { Code2 } from 'lucide-react';
import { SectionConfig } from '../types';

export const apiConfig: SectionConfig = {
  apiEnabled: {
    label: 'API 활성화',
    type: 'boolean',
    icon: Code2
  }
};
